---
name: rpi-pipeline
description: Authoritative RPI pipeline contract — phases, ordering rules, entry/exit gates, replan triggers, and role definitions. Load alongside any rpi-* skill for cross-phase rules.
---

# rpi-pipeline

Authoritative contract for the RPI skill family. Read `rpi-pipeline.yml` in this directory for the full structured contract.

Contains: phase order, required_when, handoff_to, decision checkpoints, entry gates, exit gates, ordering rules, replan triggers, and role definitions.
